// Local secrets fallback. Leave empty or set your Gemini API key here for local/dev only.
// Prefer passing the key via: --dart-define=GEMINI_API_KEY=YOUR_KEY
// Note: Do NOT commit real keys to version control.
const String kGeminiApiKeyLocal = '';
